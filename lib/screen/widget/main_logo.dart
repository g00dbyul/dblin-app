import 'package:flutter/material.dart';

class MainLogo extends StatelessWidget {
  @override
  Widget build(BuildContext) {
    return Container(
      padding: EdgeInsets.all(20),
      child: Image.asset('assets/main_logo.gif'),
    );
  }
}